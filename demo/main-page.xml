<Page xmlns="http://www.nativescript.org/tns.xsd" loaded="pageLoaded">
  <TabView>
    <TabView.items>
      <TabViewItem title="About">
        <TabViewItem.view>
          <ScrollView>
            <StackLayout cssClass="tab-content">
              <Image margin="10" src="~/res/telerik-logo.png" />
              <Label text="Mapbox plugin demo" cssClass="title"/>
              <Label text="Awesome platform independent OpenGL powered maps by Mapbox. Make sure you provide your own (free) 'accessToken' in main-view-model.js or unexpected things will happen." textWrap="true"/>
            </StackLayout>
          </ScrollView>
        </TabViewItem.view>
      </TabViewItem>
      <TabViewItem title="Demo">
        <TabViewItem.view>
          <GridLayout columns="*, *" rows="auto, auto, auto, auto" horizontalAlignment="center" cssClass="tab-content">

            <Button row="0" col="0" text="show" tap="{{ doShow }}" />
            <Button row="0" col="1"  text="hide" tap="{{ doHide }}"/>

            <Button row="1" col="0" text="get center" tap="{{ doGetCenter }}" />
            <Button row="1" col="1"  text="set center" tap="{{ doSetCenter }}" />

            <Button row="2" col="0" text="get zoom level" tap="{{ doGetZoomLevel }}" />
            <Button row="2" col="1"  text="set zoom level" tap="{{ doSetZoomLevel }}" />

            <Button row="3" col="0" text="add markers" tap="{{ doAddMarkers }}" />
            <Button row="3" col="1"  text="add polygon" tap="{{ doAddPolygon }}" />

            <Button row="4" col="0"  text="location perm?" tap="{{ doCheckHasFineLocationPermission }}" />
            <Button row="4" col="1"  text="ask permission" tap="{{ doRequestFineLocationPermission }}" />

          </GridLayout>
        </TabViewItem.view>
      </TabViewItem>
    </TabView.items>
  </TabView>
</Page>