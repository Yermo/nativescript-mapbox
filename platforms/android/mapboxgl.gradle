repositories {
    maven { url 'https://oss.sonatype.org/content/repositories/snapshots/' }
}

dependencies {
    compile('com.mapbox.mapboxgl:mapbox-gl-android-sdk:0.0.1-SNAPSHOT@aar') {
        transitive = true
    }
}